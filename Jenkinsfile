pipeline {
  agent any
  stages {
    stage('Pre-Build') {
      steps {
        echo 'Pre-build'
      }
    }

    stage('Build') {
      steps {
        echo 'Build'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy'
      }
    }

  }
}